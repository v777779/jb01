package ch16.ex11.integra;

/**
 * Created by V1 on 26.03.2017.
 */
public class ConvTo {
    public static boolean[] primitive(Boolean[] array) {
        boolean [] res = new boolean[array.length];
        for (int i = 0; i < array.length; i++) {
            res[i] = array[i]; // автоматическая распаковка
        }
        return res;
    }
    public static byte[] primitive(Byte[] array) {
        byte [] res = new byte[array.length];
        for (int i = 0; i < array.length; i++) {
            res[i] = array[i]; // автоматическая распаковка
        }
        return res;
    }
    public static char[] primitive(Character[] array) {
        char [] res = new char[array.length];
        for (int i = 0; i < array.length; i++) {
            res[i] = array[i]; // автоматическая распаковка
        }
        return res;
    }
    public static short[] primitive(Short[] array) {
        short [] res = new short[array.length];
        for (int i = 0; i < array.length; i++) {
            res[i] = array[i]; // автоматическая распаковка
        }
        return res;
    }
    public static int[] primitive(Integer[] array) {
        int [] res = new int[array.length];
        for (int i = 0; i < array.length; i++) {
            res[i] = array[i]; // автоматическая распаковка
        }
        return res;
    }
    public static long[] primitive(Long[] array) {
        long [] res = new long[array.length];
        for (int i = 0; i < array.length; i++) {
            res[i] = array[i]; // автоматическая распаковка
        }
        return res;
    }
    public static float[] primitive(Float[] array) {
        float [] res = new float[array.length];
        for (int i = 0; i < array.length; i++) {
            res[i] = array[i]; // автоматическая распаковка
        }
        return res;
    }
    public static double[] primitive(Double[] array) {
        double [] res = new double[array.length];
        for (int i = 0; i < array.length; i++) {
            res[i] = array[i]; // автоматическая распаковка
        }
        return res;
    }


}
